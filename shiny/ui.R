#### PACKAGES -----
options(encoding = "UTF-8")

library(shiny)
library(shinythemes)
library(shinyWidgets)
library(shinycssloaders)
library(DT)
# library(ggthemes)
library(plotly)
library(here)
library(scales)
library(dplyr)
library(stringr)
library(tidyr)
library(ggalt)

#### UI -----
ui <-  fluidPage(theme = shinytheme("spacelab"),
                 
# title ------ 
# shown across tabs
titlePanel("Prevalence and Incidence of Parkinsonism and subtypes"),
               
# set up: pages along the side -----  
                 navlistPanel(
                   
                   
## Introduction  -----  
tabPanel("Background", 
  tags$h3("Background"),
     tags$hr(),
   tags$h4(tags$strong("Please note, the results presented here should be considered as 
                       preliminary and subject to change.")),
   tags$hr(),
   # tags$h5("This app is a companion to the study ...."),
  HTML('<br>'),
  tags$hr()
), 
## Prevalence disease ------ 
tabPanel("Population prevalence: Disease",	  
         tags$h3("Prevalence estimates"),
         tags$h5("Prevalence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_database_name_selector",
                         label = "Database",
                         choices = unique(prevalence_estimates$database_name),
                         selected = unique(prevalence_estimates$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_outcome_cohort_name_selector",
                         label = "Outcome",
                         choices = sort(unique(prevalence_estimates$outcome_cohort_name)),
                         selected = sort(unique(prevalence_estimates$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_age_group_selector",
                         label = "Age group",
                         choices = levels(prevalence_estimates$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_sex_selector",
                         label = "Sex",
                         choices = unique(prevalence_estimates$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_days_prior_history_selector",
                         label = "Days prior history",
                         choices = unique(prevalence_estimates$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_start_date_selector",
                         label = "Prevalence start date",
                         choices = as.character(unique(prevalence_estimates$prevalence_start_date)),
                         selected = as.character(unique(prevalence_estimates$prevalence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates", 
                              DTOutput('tbl_prevalence_estimates') %>% withSpinner()), 
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_x_axis",
                                              label = "X axis",
                                              choices = c("denominator_age_group", 
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = "prevalence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_facet",
                                              label = "Facet by",
                                              choices = c("denominator_age_group", 
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_group",
                                              label = "Colour by",
                                              choices = c("denominator_age_group", 
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_prevalence_estimates', height = "800px") %>% withSpinner() ), 
                     tabPanel("Attrition table", 
                              DTOutput('tbl_prevalence_attrition') %>% withSpinner())
                     )
),
## Incidence disease------ 
tabPanel("Population incidence: Disease",	  
         tags$h3("Incidence estimates"),
         tags$h5("Incidence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_database_name_selector",
                         label = "Database",
                         choices = unique(incidence_estimates$database_name),
                         selected = unique(incidence_estimates$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_outcome_cohort_name_selector",
                         label = "Outcome",
                         choices = sort(unique(incidence_estimates$outcome_cohort_name)),
                         selected = sort(unique(incidence_estimates$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_age_group_selector",
                         label = "Age group",
                         choices = levels(incidence_estimates$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_sex_selector",
                         label = "Sex",
                         choices = unique(incidence_estimates$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_days_prior_history_selector",
                         label = "Days prior history",
                         choices = unique(incidence_estimates$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_start_date_selector",
                         label = "incidence start date",
                         choices = as.character(unique(incidence_estimates$incidence_start_date)),
                         selected = as.character(unique(incidence_estimates$incidence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates", 
                              DTOutput('tbl_incidence_estimates') %>% withSpinner()), 
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_x_axis",
                                              label = "X axis",
                                              choices = c("denominator_age_group", 
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = "incidence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_facet",
                                              label = "Facet by",
                                              choices = c("denominator_age_group", 
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_group",
                                              label = "Colour by",
                                              choices = c("denominator_age_group", 
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_incidence_estimates', height = "800px") %>% withSpinner() ), 
                     tabPanel("Attrition table", 
                              DTOutput('tbl_incidence_attrition') %>% withSpinner())
                     )
),
## Prevalence of antiparkinson drugs ------
tabPanel("Population prevalence: Drugs",
         tags$h3("Prevalence estimates"),
         tags$h5("Prevalence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_database_name_selector_drugs",
                         label = "Database",
                         choices = unique(prevalence_estimates_drugs$database_name),
                         selected = unique(prevalence_estimates_drugs$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_outcome_cohort_name_selector_drugs",
                         label = "Outcome",
                         choices = sort(unique(prevalence_estimates_drugs$outcome_cohort_name)),
                         selected = sort(unique(prevalence_estimates_drugs$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_age_group_selector_drugs",
                         label = "Age group",
                         choices = levels(prevalence_estimates_drugs$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_sex_selector_drugs",
                         label = "Sex",
                         choices = unique(prevalence_estimates_drugs$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_days_prior_history_selector_drugs",
                         label = "Days prior history",
                         choices = unique(prevalence_estimates_drugs$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_start_date_selector_drugs",
                         label = "Prevalence start date",
                         choices = as.character(unique(prevalence_estimates_drugs$prevalence_start_date)),
                         selected = as.character(unique(prevalence_estimates_drugs$prevalence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_prevalence_estimates_drugs') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_x_axis_drugs",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = "prevalence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_facet_drugs",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_group_drugs",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_prevalence_estimates_drugs', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_prevalence_attrition_drugs') %>% withSpinner())
         )
),

## Incidence of antiparkinson drugs  ------
tabPanel("Population incidence: Drugs",
         tags$h3("Incidence estimates"),
         tags$h5("Incidence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_database_name_selector_drugs",
                         label = "Database",
                         choices = unique(incidence_estimates_drugs$database_name),
                         selected = unique(incidence_estimates_drugs$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_outcome_cohort_name_selector_drugs",
                         label = "Outcome",
                         choices = sort(unique(incidence_estimates_drugs$outcome_cohort_name)),
                         selected = sort(unique(incidence_estimates_drugs$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_age_group_selector_drugs",
                         label = "Age group",
                         choices = levels(incidence_estimates_drugs$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_sex_selector_drugs",
                         label = "Sex",
                         choices = unique(incidence_estimates_drugs$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_days_prior_history_selector_drugs",
                         label = "Days prior history",
                         choices = unique(incidence_estimates_drugs$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_start_date_selector_drugs",
                         label = "incidence start date",
                         choices = as.character(unique(incidence_estimates_drugs$incidence_start_date)),
                         selected = as.character(unique(incidence_estimates_drugs$incidence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_incidence_estimates_drugs') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_x_axis_drugs",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = "incidence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_facet_drugs",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_group_drugs",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_incidence_estimates_drugs', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_incidence_attrition_drugs') %>% withSpinner())
         )
),

## Prevalence PD drugs ------
tabPanel("Population prevalence in PD: Drugs",
         tags$h3("Prevalence estimates"),
         tags$h5("Prevalence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_database_name_selector_PD",
                         label = "Database",
                         choices = unique(prevalence_estimates_drugPD$database_name),
                         selected = unique(prevalence_estimates_drugPD$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_outcome_cohort_name_selector_PD",
                         label = "Outcome",
                         choices = sort(unique(prevalence_estimates_drugPD$outcome_cohort_name)),
                         selected = sort(unique(prevalence_estimates_drugPD$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_age_group_selector_PD",
                         label = "Age group",
                         choices = levels(prevalence_estimates_drugPD$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_sex_selector_PD",
                         label = "Sex",
                         choices = unique(prevalence_estimates_drugPD$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_days_prior_history_selector_PD",
                         label = "Days prior history",
                         choices = unique(prevalence_estimates_drugPD$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_start_date_selector_PD",
                         label = "Prevalence start date",
                         choices = as.character(unique(prevalence_estimates_drugPD$prevalence_start_date)),
                         selected = as.character(unique(prevalence_estimates_drugPD$prevalence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_prevalence_estimates_PD') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_x_axis_PD",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = "prevalence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_facet_PD",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_group_PD",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_prevalence_estimates_PD', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_prevalence_attrition_PD') %>% withSpinner())
         )
),

## Incidence PD drugs ------
tabPanel("Population incidence in PD: Drugs",
         tags$h3("Incidence estimates"),
         tags$h5("Incidence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_database_name_selector_PD",
                         label = "Database",
                         choices = unique(incidence_estimates_drugPD$database_name),
                         selected = unique(incidence_estimates_drugPD$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_outcome_cohort_name_selector_PD",
                         label = "Outcome",
                         choices = sort(unique(incidence_estimates_drugPD$outcome_cohort_name)),
                         selected = sort(unique(incidence_estimates_drugPD$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_age_group_selector_PD",
                         label = "Age group",
                         choices = levels(incidence_estimates_drugPD$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_sex_selector_PD",
                         label = "Sex",
                         choices = unique(incidence_estimates_drugPD$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_days_prior_history_selector_PD",
                         label = "Days prior history",
                         choices = unique(incidence_estimates_drugPD$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_start_date_selector_PD",
                         label = "incidence start date",
                         choices = as.character(unique(incidence_estimates_drugPD$incidence_start_date)),
                         selected = as.character(unique(incidence_estimates_drugPD$incidence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_incidence_estimates_PD') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_x_axis_PD",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = "incidence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_facet_PD",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_group_PD",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_incidence_estimates_PD', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_incidence_attrition_PD') %>% withSpinner())
         )
),

## Prevalence Parkinsonism drugs ------
tabPanel("Population prevalence in Parkinsonism: Drugs",
         tags$h3("Prevalence estimates"),
         tags$h5("Prevalence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_database_name_selector_Parkinsonism",
                         label = "Database",
                         choices = unique(prevalence_estimates_drugParkinsonism$database_name),
                         selected = unique(prevalence_estimates_drugParkinsonism$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_outcome_cohort_name_selector_Parkinsonism",
                         label = "Outcome",
                         choices = sort(unique(prevalence_estimates_drugParkinsonism$outcome_cohort_name)),
                         selected = sort(unique(prevalence_estimates_drugParkinsonism$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_age_group_selector_Parkinsonism",
                         label = "Age group",
                         choices = levels(prevalence_estimates_drugParkinsonism$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_sex_selector_Parkinsonism",
                         label = "Sex",
                         choices = unique(prevalence_estimates_drugParkinsonism$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_days_prior_history_selector_Parkinsonism",
                         label = "Days prior history",
                         choices = unique(prevalence_estimates_drugParkinsonism$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_start_date_selector_Parkinsonism",
                         label = "Prevalence start date",
                         choices = as.character(unique(prevalence_estimates_drugParkinsonism$prevalence_start_date)),
                         selected = as.character(unique(prevalence_estimates_drugParkinsonism$prevalence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_prevalence_estimates_Parkinsonism') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_x_axis_Parkinsonism",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = "prevalence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_facet_Parkinsonism",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_group_Parkinsonism",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_prevalence_estimates_Parkinsonism', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_prevalence_attrition_Parkinsonism') %>% withSpinner())
         )
),

## Incidence Parkinsonism drugs ------
tabPanel("Population incidence in Parkinsonism: Drugs",
         tags$h3("Incidence estimates"),
         tags$h5("Incidence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_database_name_selector_Parkinsonism",
                         label = "Database",
                         choices = unique(incidence_estimates_drugParkinsonism$database_name),
                         selected = unique(incidence_estimates_drugParkinsonism$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_outcome_cohort_name_selector_Parkinsonism",
                         label = "Outcome",
                         choices = sort(unique(incidence_estimates_drugParkinsonism$outcome_cohort_name)),
                         selected = sort(unique(incidence_estimates_drugParkinsonism$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_age_group_selector_Parkinsonism",
                         label = "Age group",
                         choices = levels(incidence_estimates_drugParkinsonism$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_sex_selector_Parkinsonism",
                         label = "Sex",
                         choices = unique(incidence_estimates_drugParkinsonism$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_days_prior_history_selector_Parkinsonism",
                         label = "Days prior history",
                         choices = unique(incidence_estimates_drugParkinsonism$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_start_date_selector_Parkinsonism",
                         label = "incidence start date",
                         choices = as.character(unique(incidence_estimates_drugParkinsonism$incidence_start_date)),
                         selected = as.character(unique(incidence_estimates_drugParkinsonism$incidence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_incidence_estimates_Parkinsonism') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_x_axis_Parkinsonism",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = "incidence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_facet_Parkinsonism",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_group_Parkinsonism",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_incidence_estimates_Parkinsonism', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_incidence_attrition_Parkinsonism') %>% withSpinner())
         )
),

## Prevalence VP drugs ------
tabPanel("Population prevalence in VP: Drugs",
         tags$h3("Prevalence estimates"),
         tags$h5("Prevalence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_database_name_selector_VP",
                         label = "Database",
                         choices = unique(prevalence_estimates_drugVP$database_name),
                         selected = unique(prevalence_estimates_drugVP$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_outcome_cohort_name_selector_VP",
                         label = "Outcome",
                         choices = sort(unique(prevalence_estimates_drugVP$outcome_cohort_name)),
                         selected = sort(unique(prevalence_estimates_drugVP$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_age_group_selector_VP",
                         label = "Age group",
                         choices = levels(prevalence_estimates_drugVP$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_sex_selector_VP",
                         label = "Sex",
                         choices = unique(prevalence_estimates_drugVP$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_days_prior_history_selector_VP",
                         label = "Days prior history",
                         choices = unique(prevalence_estimates_drugVP$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_start_date_selector_VP",
                         label = "Prevalence start date",
                         choices = as.character(unique(prevalence_estimates_drugVP$prevalence_start_date)),
                         selected = as.character(unique(prevalence_estimates_drugVP$prevalence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_prevalence_estimates_VP') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_x_axis_VP",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = "prevalence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_facet_VP",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_group_VP",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_prevalence_estimates_VP', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_prevalence_attrition_VP') %>% withSpinner())
         )
),

## Incidence VP drugs ------
tabPanel("Population incidence in VP: Drugs",
         tags$h3("Incidence estimates"),
         tags$h5("Incidence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_database_name_selector_VP",
                         label = "Database",
                         choices = unique(incidence_estimates_drugVP$database_name),
                         selected = unique(incidence_estimates_drugVP$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_outcome_cohort_name_selector_VP",
                         label = "Outcome",
                         choices = sort(unique(incidence_estimates_drugVP$outcome_cohort_name)),
                         selected = sort(unique(incidence_estimates_drugVP$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_age_group_selector_VP",
                         label = "Age group",
                         choices = levels(incidence_estimates_drugVP$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_sex_selector_VP",
                         label = "Sex",
                         choices = unique(incidence_estimates_drugVP$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_days_prior_history_selector_VP",
                         label = "Days prior history",
                         choices = unique(incidence_estimates_drugVP$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_start_date_selector_VP",
                         label = "incidence start date",
                         choices = as.character(unique(incidence_estimates_drugVP$incidence_start_date)),
                         selected = as.character(unique(incidence_estimates_drugVP$incidence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_incidence_estimates_VP') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_x_axis_VP",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = "incidence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_facet_VP",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_group_VP",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_incidence_estimates_VP', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_incidence_attrition_VP') %>% withSpinner())
         )
),

## Prevalence DIP drugs ------
tabPanel("Population prevalence in DIP: Drugs",
         tags$h3("Prevalence estimates"),
         tags$h5("Prevalence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_database_name_selector_DIP",
                         label = "Database",
                         choices = unique(prevalence_estimates_drugDIP$database_name),
                         selected = unique(prevalence_estimates_drugDIP$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_outcome_cohort_name_selector_DIP",
                         label = "Outcome",
                         choices = sort(unique(prevalence_estimates_drugDIP$outcome_cohort_name)),
                         selected = sort(unique(prevalence_estimates_drugDIP$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_age_group_selector_DIP",
                         label = "Age group",
                         choices = levels(prevalence_estimates_drugDIP$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_sex_selector_DIP",
                         label = "Sex",
                         choices = unique(prevalence_estimates_drugDIP$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_denominator_days_prior_history_selector_DIP",
                         label = "Days prior history",
                         choices = unique(prevalence_estimates_drugDIP$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "prevalence_start_date_selector_DIP",
                         label = "Prevalence start date",
                         choices = as.character(unique(prevalence_estimates_drugDIP$prevalence_start_date)),
                         selected = as.character(unique(prevalence_estimates_drugDIP$prevalence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_prevalence_estimates_DIP') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_x_axis_DIP",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = "prevalence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_facet_DIP",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "prevalence_plot_group_DIP",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "prevalence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_prevalence_estimates_DIP', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_prevalence_attrition_DIP') %>% withSpinner())
         )
),

## Incidence DIP drugs ------
tabPanel("Population incidence in DIP: Drugs",
         tags$h3("Incidence estimates"),
         tags$h5("Incidence estimates are shown below...."),
         tags$hr(),
         tags$h5("Database and study outcome"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_database_name_selector_DIP",
                         label = "Database",
                         choices = unique(incidence_estimates_drugDIP$database_name),
                         selected = unique(incidence_estimates_drugDIP$database_name),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_outcome_cohort_name_selector_DIP",
                         label = "Outcome",
                         choices = sort(unique(incidence_estimates_drugDIP$outcome_cohort_name)),
                         selected = sort(unique(incidence_estimates_drugDIP$outcome_cohort_name))[1],
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Population settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_age_group_selector_DIP",
                         label = "Age group",
                         choices = levels(incidence_estimates_drugDIP$denominator_age_group),
                         selected = "All ages",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_sex_selector_DIP",
                         label = "Sex",
                         choices = unique(incidence_estimates_drugDIP$denominator_sex),
                         selected = "Both",
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_denominator_days_prior_history_selector_DIP",
                         label = "Days prior history",
                         choices = unique(incidence_estimates_drugDIP$denominator_days_prior_history),
                         selected = 365,
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tags$hr(),
         tags$h5("Analysis settings"),
         div(style="display: inline-block;vertical-align:top; width: 150px;",
             pickerInput(inputId = "incidence_start_date_selector_DIP",
                         label = "incidence start date",
                         choices = as.character(unique(incidence_estimates_drugDIP$incidence_start_date)),
                         selected = as.character(unique(incidence_estimates_drugDIP$incidence_start_date)),
                         options = list(
                           `actions-box` = TRUE,
                           size = 10,
                           `selected-text-format` = "count > 3"),
                         multiple = TRUE)
         ),
         tabsetPanel(type = "tabs",
                     tabPanel("Table of estimates",
                              DTOutput('tbl_incidence_estimates_DIP') %>% withSpinner()),
                     tabPanel("Plot of estimates",
                              tags$hr(),
                              tags$h5("Plotting options"),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_x_axis_DIP",
                                              label = "X axis",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = "incidence_start_date",
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = FALSE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_facet_DIP",
                                              label = "Facet by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              selected = c("outcome_cohort_name",
                                                           "database_name"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              div(style="display: inline-block;vertical-align:top; width: 150px;",
                                  pickerInput(inputId = "incidence_plot_group_DIP",
                                              label = "Colour by",
                                              choices = c("denominator_age_group",
                                                          "denominator_sex",
                                                          "denominator_days_prior_history",
                                                          "outcome_cohort_name",
                                                          "database_name",
                                                          "incidence_start_date"),
                                              options = list(
                                                `actions-box` = TRUE,
                                                size = 10,
                                                `selected-text-format` = "count > 3"),
                                              multiple = TRUE,)
                              ),
                              plotlyOutput('plot_incidence_estimates_DIP', height = "800px") %>% withSpinner() ),
                     tabPanel("Attrition table",
                              DTOutput('tbl_incidence_attrition_DIP') %>% withSpinner())
         )
)

# close -----
                                                   ))



